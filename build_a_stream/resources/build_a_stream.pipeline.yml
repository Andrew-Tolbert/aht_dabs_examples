# The main pipeline for build_a_stream
resources:
  pipelines:
    build_a_stream_pipeline:
      name: build_a_stream_pipeline
      ## Specify the 'catalog' field to configure this pipeline to make use of Unity Catalog:
      # catalog: catalog_name
      schema: build_a_stream_${bundle.target}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
